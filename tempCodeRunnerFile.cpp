for(int i = n - 1; i > 0; i--){
    //     //Đưa phần tử lớn nhất về cuối mảng
    //     swap(a[0], a[i]);
    //     //Gọi heapify để tạo heap max cho phần còn lại
    //     heapify(a, i, 0);
    // }